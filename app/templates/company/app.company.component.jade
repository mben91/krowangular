.col-md-6.col-md-offset-3
    h1 Home
    p You're logged in with JWT!!
    div
        | Users from secure api end point:
        ul
            li(*ngFor="let user of users") {{user.firstName}} {{user.lastName}}
        
    p
        a([routerLink]="['/login']") Logout